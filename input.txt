#define SKIP_SPACES(p, limit)  \
{ char *lim = (limit);         \
  while (p < lim) {            \
	if (*p++ != ' ') {         \
	  p--; break; }}}

int main(){
	int * a = {1,2,3};
	if (*a != 0)
	  SKIP_SPACES (a, aaa1);
	else
		printf("it works!\n");
	return 0;
}